# shayyed_engine root URL (no trailing slash) — match engine PORT (default 4000)
VITE_API_BASE_URL=http://localhost:4000

# Same value as engine .env APIKEY (required for POST /admin/auth/signin)
VITE_API_KEY=
