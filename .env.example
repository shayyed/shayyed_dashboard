# shayyed_engine root URL (no trailing slash) — match engine PORT (3300 avoids Windows reserved 3436–3535)
VITE_API_BASE_URL=http://localhost:3300

# Same value as engine .env APIKEY (required for POST /admin/auth/signin)
VITE_API_KEY=
