# shayyed_engine root URL (no trailing slash) — match engine PORT (default 3000)
VITE_API_BASE_URL=http://localhost:3000

# Same value as engine .env APIKEY (required for POST /admin/auth/signin)
VITE_API_KEY=
